
    <h1>タスクリスト一覧</h1>
    
    <ul>
        <% @tasks.each do |task| %>
            <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></li>
            
        <% end %>
        
    </ul>
    
    <%= link_to 'タスクを作成する', new_task_path %>